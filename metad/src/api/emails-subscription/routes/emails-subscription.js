'use strict';

/**
 * emails-subscription router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::emails-subscription.emails-subscription');
